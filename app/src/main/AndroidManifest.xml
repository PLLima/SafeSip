<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.safesip">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/safesip"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/safesip_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeSip">

        <activity
            android:name=".MoreInfoActivity"
            android:exported="false" />
        <activity
            android:name=".AdviceActivity"
            android:exported="false" />

        <receiver android:name=".MidnightReceiver" />

        <activity android:name=".Graph" />
        <activity android:name=".MotivQuoteActivity" />
        <activity android:name=".PersonalInformation" />
        <activity android:name=".ActionActivity" />
        <activity android:name=".RegisterDrinkActivity" />
        <activity android:name=".TooMuchAlcohol" />
        <activity android:name=".HistoryActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>